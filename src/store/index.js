import { createStore } from 'vuex';

const store = createStore({
  state: {
    // Aquí se almacena el estado de la aplicación
  },
  mutations: {
    // Aquí se definen las mutaciones para cambiar el estado
  },
  actions: {
    // Aquí se definen las acciones que pueden llamar a mutaciones
  },
  getters: {
    // Aquí se definen los getters para acceder al estado
  }
});

export default store;
